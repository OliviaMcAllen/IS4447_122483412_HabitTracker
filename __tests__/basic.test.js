// Basic Jest test
// Used to confirm that the testing environment is set up correctly

test('basic test works', () => {
  // Simple assertion to check that Jest is running properly
  expect(1 + 1).toBe(2);
});